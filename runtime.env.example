# Secret-free local defaults. Copy to runtime.env only when needed.
TAG__ENVIRONMENT=development
TAG__HTTP__ADDR=127.0.0.1:8090
TAG__MONGO__URI=mongodb://127.0.0.1:27017
TAG__MONGO__DATABASE=tos_tag
TAG__SLACK__MODE=stub
TAG__SLACK__LIVE_ENABLED=false
# Future live initiative only. Load tokens from a secret store, never this file.
TAG__SLACK__ORGANIZATION_ID=
TAG__SLACK__TEAM_ID=
TAG__SLACK__APP_TOKEN=
TAG__SLACK__BOT_TOKEN=
TAG__SLACK__BOT_USER_ID=
TAG__AUTH__ENABLED=false
# Required when auth is enabled; load the real value from a secret store.
TAG__AUTH__ADMIN_TOKEN=
TAG__GATING__MODE=shadow
TAG__OPENCODE__ENABLED=false
TAG__OPENCODE__MODE=local_worker
TAG__OPENCODE__BASE_URL=http://127.0.0.1:4096
TAG__OPENCODE__USERNAME=opencode
# Required only for explicitly selected external mode.
TAG__OPENCODE__PASSWORD=
TAG__OPENCODE__COMMAND=opencode
TAG__OPENCODE__WORKER_ROOT=/tmp/tos-tag-workers
TAG__MODELS__DEFAULT_PROFILE=fake-default
TAG__MODELS__DEFAULT_PROVIDER=fake
TAG__MODELS__DEFAULT_MODEL=deterministic
TAG__MODELS__DEFAULT_VARIANT=
# Example: point at telemetryos-agent-skills without copying it into this repo.
TAG__MARKETPLACES__SKILL_ROOT=
TAG__MARKETPLACES__CATALOG_PATH=.claude-plugin/marketplace.json
# Explicit allowlist. A single environment value injects one skill; use the
# tag config TOML array when selecting more than one.
TAG__MARKETPLACES__INJECTED_SKILLS=
# Proposed telemetryos-agent-tools catalog (SKILL.md + tool.json + reviewed script).
TAG__MARKETPLACES__TOOL_ROOT=
TAG__MARKETPLACES__TOOL_CATALOG_PATH=catalog.json
# Explicitly enables reviewed server-side helper execution. Requires local
# OpenCode, a configured tool marketplace, and the encrypted keystore.
TAG__MARKETPLACES__TOOLS_ENABLED=false
# Explicit tool-ID allowlist. One value is supported in ENV; use the config
# TOML array for multiple reviewed tools.
TAG__MARKETPLACES__INJECTED_TOOLS=
# Optional write-only Mongo keystore. Generate 32 random bytes and base64 them;
# inject the value from an external secret store and never commit it.
TAG__KEYSTORE__ENABLED=false
TAG__KEYSTORE__MASTER_KEY=
